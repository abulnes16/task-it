import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Logo = () => {
  return (
    <View style={styles}>
      <Text>Logo</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Logo;
